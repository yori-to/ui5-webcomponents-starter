import Element from '../Element.js';
import library from '../library.js';
var DOMAttribute = Element.extend('sap.ui.core.tmpl.DOMAttribute', {
    metadata: {
        library: 'sap.ui.core',
        properties: {
            name: {
                type: 'string',
                group: 'Data',
                defaultValue: null
            },
            value: {
                type: 'string',
                group: 'Data',
                defaultValue: null
            }
        }
    }
});
DOMAttribute.prototype.setValue = function (sValue) {
    this.setProperty('value', sValue, true);
    var oParent = this.getParent(), $this = oParent && oParent.$();
    if ($this && $this.length > 0) {
        $this.attr(this.getName(), this.getProperty('value'));
    }
    return this;
};
export default DOMAttribute;